#include <stdio.h>
#include<string.h>

int main()
{
    int i,m=0;
   char name[100];
   printf("Enter the string in five letters:\n");
   scanf("%[^\n]s",name);
   for(i=0;name[i]!='\0';i++)
    {
        m++;
    }
    for(i=m-1;i>=0;i--)
    {
        printf("%c\n",name[i]);
    }
return 0;
}
