 #include<stdio.h>
 void main()
 {
    int i;
    char str[10],str1[10];
    scanf("%s",str);
    for(i=0;str[i]!='\0';i++)
    {
        str1[i]=str[i];
    }
    str1[i]='\0';
    printf("copied string %s",str1);

 }
